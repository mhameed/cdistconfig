# export CDIST_ORDER_DEPENDENCY=on
hostname=$(cat "$__global/explorer/hostname")

find $__manifest/common/  $__manifest/${hostname}/  -type f -iname "*.sh" | while read manifest; do
    . "$manifest"
done
